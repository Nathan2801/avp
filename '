const Parser = (props) => ({
	text: props?.text,
	consumed: null,
	parsed: null,
	error: "",
})

Parser.consume = (parser) => Parser({
	text: parser.text.slice(1),
	consumed: parser.text[0],
})

Parser.consumeDigit = (parser) => Parser({
	text: parser.text.slice(1),
})
